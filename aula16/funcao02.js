function somar(n1 = 0, n2 = 0) { // Colocar "= 0" depois do parâmetro serve para indicar um valor padrão caso o mesmo não seja indicado na chamada para a ação, no caso, quando a chamada só passa 1 número ou nenhum
    return n1 + n2
}

// let res = somar(24, 294)
// console.log(res)

console.log(somar(24, 295))